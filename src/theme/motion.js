const motion = Object.freeze({
  duration: Object.freeze({
    fast: 120,
    normal: 180,
    medium: 220,
    slow: 300,
  }),
  stagger: Object.freeze({
    card: 35,
    list: 50,
  }),
  scale: Object.freeze({
    pressIn: 0.98,
    pressOut: 1,
  }),
});

export default motion;
